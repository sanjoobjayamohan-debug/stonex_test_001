rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allows seamless fallback/development database access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
